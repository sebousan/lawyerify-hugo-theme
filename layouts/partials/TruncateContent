{{ $limit := .limit | default 120 }}
{{ $content := .content }}

{{ return
  safeHTML (
    plainify (
      chomp (
        truncate $limit "…" (
          partial "CorrectPunctuation" $content
        )
      )
    ) 
  ) 
}}